"use client";import{useMemo,useState}from"react";import{Check,Plus}from"lucide-react";import{Button}from"@/components/ui/button";import{FoodsLink}from"@/components/pages/foods-link";import{useFoodsQuoteRequest}from"@/features/quote-request";import{processedPlantPrograms,processedPlantQuoteOptions}from"../data/processed-plant-knowledge";
export function ProcessedPlantSpecBuilder(){const[key,setKey]=useState<string>(processedPlantPrograms[0].key);const{items,addItem,removeItem}=useFoodsQuoteRequest();const option=useMemo(()=>processedPlantQuoteOptions.find((item)=>item.productKey===key)!,[key]);const program=processedPlantPrograms.find((item)=>item.key===key)!;const active=items.some((item)=>item.slug===option.slug);const product={slug:option.slug,title:option.title,category:"Dehydrated & Processed",subCategory:"Processed Plant Foods",form:program.form,grade:"Identity, formulation and lot evidence required",originState:"Exact processor and shipment lot to confirm"};return <div className="rounded-[2rem] border border-primary/20 bg-card p-6 sm:p-8"><p className="section-kicker">Build a processed-food request</p><h2 className="mt-3 text-3xl font-bold tracking-[-0.04em]">Choose the identity before formula and performance limits.</h2><label className="mt-7 block max-w-xl text-sm font-bold">Plant-food program<select value={key} onChange={(e)=>setKey(e.target.value)} className="mt-2 h-12 w-full rounded-xl border border-border bg-background px-4">{processedPlantPrograms.map((item)=><option key={item.key} value={item.key}>{item.name}</option>)}</select></label><div className="mt-4 max-w-3xl rounded-xl bg-primary/5 p-4 text-sm leading-6"><strong>{program.form}.</strong> {program.identity}</div><div className="mt-6 flex flex-col gap-3 sm:flex-row"><Button type="button" className="h-12 bg-accent font-bold text-accent-foreground" onClick={()=>active?removeItem(option.slug):addItem(product)}>{active?<><Check className="mr-2 size-4"/>In RFQ list (remove)</>:<><Plus className="mr-2 size-4"/>Add plant-food program</>}</Button><Button asChild variant="outline" className="h-12"><FoodsLink href={`/catalog/${option.slug}`}>Review evidence gate</FoodsLink></Button></div></div>}
