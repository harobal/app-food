"use client";import{useMemo,useState}from"react";import{Check,Plus}from"lucide-react";import{Button}from"@/components/ui/button";import{FoodsLink}from"@/components/pages/foods-link";import{useFoodsQuoteRequest}from"@/features/quote-request";import{teaPrograms,teaQuoteOptions}from"../data/tea-knowledge";
export function TeaSpecBuilder(){const[key,setKey]=useState<string>(teaPrograms[0].key);const{items,addItem,removeItem}=useFoodsQuoteRequest();const option=useMemo(()=>teaQuoteOptions.find((item)=>item.productKey===key)!,[key]);const identity=teaPrograms.find((item)=>item.key===key)!;const active=items.some((item)=>item.slug===option.slug);const product={slug:option.slug,title:option.title,category:"Tea",subCategory:identity.type,form:"Dry Leaf",grade:"Specification and evidence required",originState:"Exact garden/factory and invoice lot to confirm"};return <div className="rounded-[2rem] border border-primary/20 bg-card p-6 sm:p-8"><p className="section-kicker">Build a tea request</p><h2 className="mt-3 text-3xl font-bold tracking-[-0.04em]">Choose tea identity and manufacturing path.</h2><label className="mt-7 block max-w-xl text-sm font-bold">Tea program<select value={key} onChange={(e)=>setKey(e.target.value)} className="mt-2 h-12 w-full rounded-xl border border-border bg-background px-4">{teaPrograms.map((item)=><option key={item.key} value={item.key}>{item.name}</option>)}</select></label><div className="mt-4 max-w-3xl rounded-xl bg-primary/5 p-4 text-sm leading-6"><strong>{identity.claim}.</strong> {identity.identity}</div><div className="mt-6 flex flex-col gap-3 sm:flex-row"><Button type="button" className="h-12 bg-accent font-bold text-accent-foreground" onClick={()=>active?removeItem(option.slug):addItem(product)}>{active?<><Check className="mr-2 size-4"/>In RFQ list (remove)</>:<><Plus className="mr-2 size-4"/>Add tea program</>}</Button><Button asChild variant="outline" className="h-12"><FoodsLink href={`/catalog/${option.slug}`}>Review evidence gate</FoodsLink></Button></div></div>}
